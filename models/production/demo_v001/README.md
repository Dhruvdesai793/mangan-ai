# Production -- demo_v001 (Status: DEMO)
Simulation module. Returns scenario-consistent target/predicted tonnage and a
shortfall probability. Replace with a real forecasting model (e.g. time-series
or regression on historical MOIL production logs) once that data exists.
