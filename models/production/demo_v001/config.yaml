model_id: production
version: demo_v001
status: DEMO
type: SIMULATION
input_mode: scenario
data_source_label: SYNTHETIC_SCENARIO
note: Retained only as the legacy scenario simulator; not the selected registry version when production/v001 is present.
